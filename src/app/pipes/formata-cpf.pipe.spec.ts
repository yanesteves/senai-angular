import { FormataCPFPipe } from './formata-cpf.pipe';

describe('FormataCPFPipe', () => {
  it('create an instance', () => {
    const pipe = new FormataCPFPipe();
    expect(pipe).toBeTruthy();
  });
});
